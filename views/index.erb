<!DOCTYPE>
<html>
  <head>
    <title>Motocross</title>

    <style type="text/css">
      html, body {
        margin: 0;
        padding: 0;
        font-family: helvetica, ariel, san-serif;
      }
      a {
        text-decoration: none
      }
      .container {
        position: relative;
        width: 100%;
        height: 100%
      }
      .sidebar {
        background: #ebebeb;
        width: 20%;
        height: 100%;
        float: left
      }
      .track-info {
        width: 80%;
        height: 100%;
        float: left
      }
      ul {
        list-style: none;
      }
      li {
        margin-bottom: 10px;
      }
      table {
        width: 100%;
        text-align: center;
      }
      td {
        border-bottom: 1px solid #ebebeb;
        border-right: 1px solid #ebebeb
      }
      tr td:last-child {
        border-right: none
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="sidebar">
        <ul>
          <% tracks._embedded.each do |track| %>
            <li>
              <a href="/tracks/<%= track.id %>"><%= track.name %></a>
            </li>
          <% end %>
      </div>
      <div class="track-info">
        <table>
          <thead>
            <tr>
              <th>Rider Name</th>
              <th>Number</th>
              <th>Fastest Laptime</th>
            </tr>
          </thead>
          <tbody>
            <% laptimes(@id).riderData.each do |rider| %>
              <tr>
                <td><%= rider.name %></td>
                <td><%= rider.number %></td>
                <td><%= get_fastest_laptime(rider) %> Minutes</td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </body>
</html>
